import { getUserSupabase, requireUser } from "@/lib/auth";
export const dynamic="force-dynamic";
export async function GET(request:Request){try{const user=await requireUser(request),db=getUserSupabase(request);if(!user||!db)return Response.json({error:"Unauthorized"},{status:401});const{data,error}=await db.from("generations").select("*").eq("user_id",user.id).order("created_at",{ascending:false}).limit(100);if(error)throw error;return Response.json((data??[]).map(r=>({id:r.id,title:r.title,contentType:r.content_type,prompt:r.prompt,tone:r.tone,content:r.content,status:r.status,createdAt:r.created_at})))}catch(e){return Response.json({error:errorMessage(e,"Unable to load content.")},{status:500})}}
export async function DELETE(request:Request){try{const user=await requireUser(request),db=getUserSupabase(request);if(!user||!db)return Response.json({error:"Unauthorized"},{status:401});const id=new URL(request.url).searchParams.get("id");if(!id)return Response.json({error:"Missing id."},{status:400});const{error,count}=await db.from("generations").delete({count:"exact"}).eq("id",id).eq("user_id",user.id);if(error)throw error;if(!count)return Response.json({error:"Content was not found or is not owned by this account."},{status:404});return new Response(null,{status:204})}catch(e){return Response.json({error:errorMessage(e,"Delete failed.")},{status:500})}}
function errorMessage(error:unknown,fallback:string){if(error instanceof Error)return error.message;if(error&&typeof error==="object"&&"message" in error)return String(error.message);return fallback}
