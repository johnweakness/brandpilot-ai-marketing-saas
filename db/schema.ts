import { pgTable, text, timestamp, uuid } from "drizzle-orm/pg-core";
export const brandProfiles=pgTable("brand_profiles",{id:uuid("id").defaultRandom().primaryKey(),name:text("name").notNull(),website:text("website"),description:text("description").notNull(),audience:text("audience"),personality:text("personality"),avoidWords:text("avoid_words"),createdAt:timestamp("created_at",{withTimezone:true}).defaultNow().notNull(),updatedAt:timestamp("updated_at",{withTimezone:true}).defaultNow().notNull()});
export const generations=pgTable("generations",{id:uuid("id").defaultRandom().primaryKey(),title:text("title").notNull(),contentType:text("content_type").notNull(),prompt:text("prompt").notNull(),tone:text("tone").notNull(),content:text("content").notNull(),status:text("status").default("Ready").notNull(),createdAt:timestamp("created_at",{withTimezone:true}).defaultNow().notNull()});
